Must keep it ultra simple

low level words are simple
high level words support omni-debugging

16 bit words
  indexed table into 64 bit pointers

8 bit characters only
  formatting characters separate from display characters
  order
    numbers
    capitol letter
    lower letter
    punctuation
    math
    boundaries

as concise as shell scripting if possible
  env=e2
  escapes allowed
  quotes only needed as an alternative to escaped special characters

blocks
  {} code
  [] array of data
  ${} var
  $() output of command

